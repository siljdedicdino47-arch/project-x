fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.40
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
httpx>=0.27,<0.30
python-multipart>=0.0.9
pypdf>=4.3,<6.0
python-docx>=1.1,<2.0
numpy>=1.26,<3.0
# Optional — only needed for "My AI" (Training Studio tab): a small custom
# local language model, trained on your own data, no Anthropic/Ollama/network
# calls. The app runs fine without this; My AI just won't be available.
torch>=2.2,<3.0
